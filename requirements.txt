Django>=4.0,<5.0
psycopg2>=2.9.0  # PostgreSQL adapter for Python
gunicorn>=20.0.0  # WSGI HTTP server for production (optional for local dev)
pytest>=7.0.0  # Testing framework
pytest-django>=4.0.0  # pytest plugin for Django
